apiVersion: v1
  kind: Pod
  metadata:
    labels:
      app: jenkins-slave
  spec:
    containers:
      - name: docker
        image: docker
        command:
          - cat
        tty: true
      - name: node
        image: node:8
        command:
          - cat
        tty: true